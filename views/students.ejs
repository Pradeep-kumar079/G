<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
Here’s a further **minimized and mobile-optimized version** of your CSS — with even smaller font sizes, padding, and tighter spacing to ensure **everything fits comfortably** and is fully **visible on small mobile devices**:

```css
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 10px;
  background: #ffffff;
  text-align: center;
}

.h2 {
  background: linear-gradient(135deg, #1e1e1e, #444);
  color: white;
  padding: 6px;
  border-radius: 5px;
  width: 95%;
  max-width: 400px;
  margin: 10px auto;
  font-size: 14px;
}

.branch {
  color: #544d4d;
  padding: 1px;
  border-radius: 5px;
  margin: 5px auto;
  width: 130px;
  height: 35px;
}
.branch h2 {
  font-size: 13px;
  margin: 5px 0;
}

.profile-container {
  display: flex;
  flex-wrap: nowrap;
  overflow-x: auto;
  gap: 8px;
  justify-content: center;
  padding: 8px;
  max-width: 100%;
}

.profile-card {
  background: #fff;
  padding: 6px;
  border-radius: 6px;
  box-shadow: 1px 1px 6px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 80px;
  max-width: 90px;
}

.profile-card img {
  width: 35px;
  height: 35px;
  border-radius: 50%;
  object-fit: cover;
  border: 1px solid black;
  margin-bottom: 4px;
}

.profile-card p {
  font-size: 10px;
  margin: 0;
}

.vbtns {
  margin-top: 4px;
}

.vbtns button {
  padding: 3px 6px;
  font-size: 9px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
.vbtns button:hover {
  background: #0056b3;
}

.back-btn {
  display: block;
  margin: 15px auto;
  padding: 6px 10px;
  background: gray;
  color: white;
  border: none;
  border-radius: 4px;
  font-size: 12px;
}

.back-btn:hover {
  background: darkgray;
}

.nostudent {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  background: rgb(179, 63, 63);
  color: white;
  height: 35px;
  width: 90%;
  max-width: 350px;
  border-radius: 6px;
  margin: 10px auto;
  font-weight: bold;
}

.user-table {
  width: 100%;
  margin: 15px auto;
  border-collapse: collapse;
  background: white;
  border-radius: 6px;
  font-size: 11px;
}

.user-table th,
.user-table td {
  padding: 6px;
  text-align: center;
  border: 1px solid #ddd;
}

.user-table th {
  background: #acb1b7;
  color: white;
  font-size: 12px;
}

.user-table img {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  object-fit: cover;
  border: 1px solid black;
}

.connect-btn {
  padding: 3px 6px;
  font-size: 10px;
  border: none;
  border-radius: 4px;
  color: white;
  cursor: pointer;
}

.connect-btn.connect {
  background-color: green;
}
.connect-btn.disconnect {
  background-color: red;
}

/* Extra Mobile Tweaks */
@media (max-width: 400px) {
  .h2 {
    font-size: 13px;
  }

  .branch {
    width: 120px;
    height: 32px;
  }

  .branch h2 {
    font-size: 12px;
  }

  .user-table {
    font-size: 10px;
  }

  .connect-btn {
    font-size: 9px;
  }

  .back-btn {
    font-size: 11px;
    padding: 5px 8px;
  }
}
 
  </style>
</head>
<body>
  <div class="h2">
    <h2>Students from Graduate Year: <%= year %></h2>
  </div>
  
  <% if (Object.keys(users).length > 0) { %>
    <% Object.entries(users).forEach(([branchName, userList]) => { %>
        <div class="branch">
            <h3><%= branchName %> Branch</h3>
        </div>

        <% if (userList.length > 0) { %>
            <table class="user-table">
                <thead>
                    <tr>
                        <th>S.No.</th>
                        <th>Profile</th>
                        <th>Username</th>
                        <th>USN</th>
                        <th>Email</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% let serialNo = 1; %>
                    <% userList.forEach(user => { %>
                        <tr>
                            <td><%= serialNo++ %></td>
                            <td>
                                <% if (user.profileimg) { %>
                                    <img src="/upload/<%= user.profileimg %>" alt="Profile Picture">
                                <% } %>

                                <% 
                                    currentUser.connections = currentUser.connections || []; 
                                    const connectedUsers = currentUser.connections.map(conn => conn._id ? conn._id.toString() : conn);
                                    const isConnected = connectedUsers.includes(user._id.toString());
                                %>

                                <% if (isConnected) { %>
                                    <div class="vbtns">
                                        <a href="/profile/<%= user._id %>">
                                            <button>View Profile</button>
                                        </a>
                                    </div>
                                <% } %>
                            </td>
                            <td><%= user.username %></td>
                            <td><%= user.usn %></td>
                            <td><%= user.email %></td>
                            <td>
                                <% 
                                    const isCurrentUser = currentUser._id.toString() === user._id.toString();
                                %>

                                <% if (isCurrentUser) { %>
                                    <button type="button" class="connect-btn" disabled style="background-color: orange;">Myself</button>
                                <% } else { %>
                                    <form action="<%= isConnected ? '/disconnect' : '/connect' %>" method="post">
                                        <input type="hidden" name="receiverId" value="<%= user._id.toString() %>">
                                        <% if (isConnected) { %>
                                            <button type="submit" class="connect-btn disconnect">Disconnect</button>
                                        <% } else { %>
                                            <button type="submit" class="connect-btn connect">Connect</button>
                                        <% } %>
                                    </form>
                                <% } %>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <div class="nostudent">
                <p>No students found in this branch.</p>
            </div>
        <% } %>
    <% }); %>
<% } else { %>
    <div class="nostudent">
        <p>No students found for this year.</p>
    </div>
<% } %>

<a href="/home" class="back-btn">Back to Graduate Years</a>



</body>
</html>
